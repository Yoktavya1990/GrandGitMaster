"""GrandGitMaster — the king of Git repos.

A zero-dependency second brain that collects Git repositories from anywhere
(X.com, files, agents, humans), runs them through "the Ringer" (a scoring
pipeline), categorises them, and crowns the best repo per category.

Runs locally, in the cloud, on your phone (PWA), and inside any coding agent
(MCP). Python 3.9+, standard library only.
"""

__version__ = "0.1.0"
